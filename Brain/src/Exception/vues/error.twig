<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        * {
            margin: 0;
            padding: 0;
        }

        body {
            position: relative;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            width: 100%;
            height: 100vh;
            font-family: consolas, "Bahnschrift Light", Verdana, Geneva, Tahoma, sans-serif;
        }

        .container {
            position: relative;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 200px;
            animation: anim .5s ease-in-out infinite alternate;
        }

        .container h1 {
            position: absolute;
            font-size: 200px;
            color: rgb(26, 27, 27);
        }

        .container h1:nth-child(1) {
            color: rgb(5, 255, 255);
            transform: rotate(2deg);
        }
        .container h1:nth-child(2) {
            color: rgb(255, 209, 5);
            transform: rotate(-2deg);
        }
        .container h1:nth-child(3) {
            color: rgb(252, 7, 178);
            transform: rotate(-4deg);
        }

        .message {
            font-size: 16px;
            font-weight: 600;
            color: rgb(26, 27, 27);
        }

        @keyframes anim {
            0% { transform: translate(-1px, 1px); }
            25% { transform: translate(1px, 1px); }
            75% { transform: translate(-1px, -1px); }
            100% { transform: translate(1px, -1px); }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>{{ code }}</h1>
        <h1>{{ code }}</h1>
        <h1>{{ code }}</h1>
        <h1>{{ code }}</h1>
    </div>
    <div class="message">``{{ reason }}``</div><br>
   <div class="message">{{ message }}</div><br>
</body>
</html>
