{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "server PHP",
            "type": "shell",
            "command": "php -S localhost:8000",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "composer dump",
            "type": "shell",
            "command": "composer dump -o"
        },
        {
            "label": "composer update",
            "type": "shell",
            "command": "composer update"
        }
    ]
}
